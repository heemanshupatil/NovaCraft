export const metadata = {
  title: 'Pricing — NovaCraft Digital',
  description: 'Transparent INR pricing for web development and social media marketing. No hidden fees.',
};

export default function PricingLayout({ children }) {
  return children;
}
